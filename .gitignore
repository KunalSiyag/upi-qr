node_modules
.astro
dist
.vercel
.output
*.log

# Private Strategy & Agent Docs
docs/
.agents/
*.md
!README.md
# Blog/content markdown must be committed — otherwise CI builds
# without the posts and link_audit fails on inbound hrefs.
!src/content/**/*.md

# Sensitive Credentials & Keys
service_account.json
*.json.key
.env
.env.*
*.env
*.pem
*.key
.DS_Store

# Proprietary / IP packages (local only)
gumroad_package/
gumroad_seo_agent_skill.zip
.env*.local
.syndication-state.json
.indexing_state.json
